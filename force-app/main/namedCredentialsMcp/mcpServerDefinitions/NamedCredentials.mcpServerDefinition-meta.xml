<?xml version="1.0" encoding="UTF-8"?>
<McpServerDefinition xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Guarded HTTP callouts through the org&apos;s Named Credentials. Lets an agent list available credentials, describe how to call one, and send requests — with per-credential guardrails enforced server-side so the token never reaches the AI.</description>
    <masterLabel>Named Credentials</masterLabel>
    <tools>
        <apiDefinition>
            <apiIdentifier>aa:apex-ncMcp_ListCredentialsTool</apiIdentifier>
            <apiSource>API_CATALOG</apiSource>
            <operation>ncMcp_ListCredentialsTool</operation>
        </apiDefinition>
        <descriptionOverride>Lists the Named Credentials exposed through this server and whether each is ready to use. Each entry has the credential&apos;s name, isConfigured (true when it is authenticated for the current context), its base URL, and its principal type. Call this first to discover credentials; it does not include usage details — call the describe tool on one credential to learn how to call it.</descriptionOverride>
        <destructive>false</destructive>
        <idempotent>false</idempotent>
        <openWorld>false</openWorld>
        <readOnly>true</readOnly>
        <returnDirect>false</returnDirect>
        <toolName>ncMcp_ListCredentialsToolapex_ncMcp_ListCredentialsTool</toolName>
        <toolTitle>List Credentials</toolTitle>
    </tools>
    <tools>
        <apiDefinition>
            <apiIdentifier>aa:apex-ncMcp_SendRequestTool</apiIdentifier>
            <apiSource>API_CATALOG</apiSource>
            <operation>ncMcp_SendRequestTool</operation>
        </apiDefinition>
        <descriptionOverride>Sends an HTTP request to an external API through one of the org&apos;s Named Credentials. Salesforce injects authentication; credentials and tokens are never exposed. Only credentials returned by the listCredentials tool can be called; the describe tool shows how to call each one. Fails with an error on guardrail violations and on HTTP 4xx/5xx responses (the error message contains the HTTP status and response body).</descriptionOverride>
        <destructive>false</destructive>
        <idempotent>false</idempotent>
        <openWorld>true</openWorld>
        <readOnly>false</readOnly>
        <returnDirect>false</returnDirect>
        <toolName>ncMcp_SendRequestToolapex_ncMcp_SendRequestTool</toolName>
        <toolTitle>Send Request</toolTitle>
    </tools>
    <tools>
        <apiDefinition>
            <apiIdentifier>aa:apex-ncMcp_DescribeTool</apiIdentifier>
            <apiSource>API_CATALOG</apiSource>
            <operation>ncMcp_DescribeTool</operation>
        </apiDefinition>
        <descriptionOverride>Returns how to use one Named Credential API: its purpose, base URL, reference documentation URL, allowed HTTP methods, and allowed endpoint path patterns. Call listCredentials first to discover credentials, and call this before sendRequest. Fails with an error when the credential is unknown or not authenticated.</descriptionOverride>
        <destructive>false</destructive>
        <idempotent>false</idempotent>
        <openWorld>false</openWorld>
        <readOnly>true</readOnly>
        <returnDirect>false</returnDirect>
        <toolName>ncMcp_DescribeToolapex_ncMcp_DescribeTool</toolName>
        <toolTitle>Describe Named Credential</toolTitle>
    </tools>
</McpServerDefinition>
